install
  Create Users table on ios database of mysql.
  
  client build:
    `npm install` on root directory
    `npm start`
  Server build:
    `cd server` on root directory
    `node index`


    